{
  "mcpServers": {
    "TmaiAPI": {
      "command": "uv",
      "args": ["--directory", "/data/sources/tokens-mcp", "run", "mcp", "run", "server.py"],
      "env": {
        "TOKEN_METRICS_API_KEY": "tm-?????"
      }
    }
  }
}
