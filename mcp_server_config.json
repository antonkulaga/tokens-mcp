{
  "mcpServers": {
    "TmaiAPI": {
      "command": "uv",
      "args": ["--directory", "/data/sources/token-mcp", "run", "mcp", "run", "server.py"],
      "env": {
        "TOKEN_METRICS_API_KEY": "tm-?????"
      }
    }
  }
}
